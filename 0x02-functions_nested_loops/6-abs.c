#include "main.h"

/**
 * int _abs - computes the absolute value of an integer.
 *
 * Return: the absolute value of an integer
 */

int _abs(int s)
{
	if (s >= 0)
		return (s);
	else
		return (-s);
}
