#include <stdio.h>

/**
 * main - program that prints its name, followed by a new line.
 * @argc: the number of argument
 * @argv: array of strings/arguments which are pointers
 *
 * Return: Always o.
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
